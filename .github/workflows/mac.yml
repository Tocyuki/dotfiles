name: Mac OS

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: checkout
        uses: actions/checkout@v2

      - name: initialize
        run: make init

      - name: deploy
        run: make deploy
